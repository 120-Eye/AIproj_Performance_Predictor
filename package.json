{
  "name": "student-performance-dashboard",
  "version": "1.0.0",
  "description": "Student Performance Visualization Dashboard with AI insights",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@google/generative-ai": "^0.2.1"
  }
}

